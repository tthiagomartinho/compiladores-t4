/*
  Algoritmo Teste 4
*/
  
algoritmo teste3;

variaveis
	i, j : inteiro;
	x, y : inteiro;
	l, k : logico;
fim-variaveis


inicio
	x = leia();
	y = x^2 ;
	enquanto y > 0 faca
		y--;
		imprima("Tempo restante ");
		imprima(y);
	fim-enquanto

	faca:
		y--;
		imprima("Tempo restante ");
		imprima(y);
	enquanto (y > 0)
	fim-enquanto

	x = x % y;
	l = x > 7;
	k = x < 10;
	l = nao (l) ou k;
	l = x == 3;

	imprima("l eh ");
	imprima(l);

fim
