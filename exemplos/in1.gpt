/*
  Algoritmo Teste 1
*/
  
algoritmo teste1;

variaveis
  valor1, valor2 : real;
  res : inteiro;
  operacao : inteiro;
  mat : matriz[10][10][10] de inteiro;
  lit : literal;
  l : logico;
fim-variaveis

funcao adicao (v1 : inteiro, v2 : inteiro) : inteiro
  total : inteiro;
  inicio
    total = v1 + v2;
    retorne total;
  fim
fim-funcao

funcao adicao2 (v1 : inteiro, v2 : inteiro) : inteiro
  total : inteiro;
  inicio
    total = v1 + v2;
    retorne (total);
  fim
fim-funcao

funcao aleatoria () : literal
  total : inteiro;
  inicio
    retorne "total";
  fim
fim-funcao

inicio
  
  //lit = leia();
//  operacao = 5;
 // operacao = adicao(1, 2);

  operacao = central(4, 5, 1);

  //lit = aleatoria();

 // imprima(operacao);
 // imprima(lit);
fim